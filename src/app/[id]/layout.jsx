import React from 'react';

const FreindDetailsLayout = ({children}) => {
    return (
         <div className='bg-gray-100 '>
            {children}
         </div>
    );
};

export default FreindDetailsLayout;